{
  "name": "habit-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Local habit tracking app prototype.",
  "scripts": {
    "start": "PYTHONPATH=backend .venv/bin/uvicorn src.main:app --host 127.0.0.1 --port 5000",
    "dev": "PYTHONPATH=backend .venv/bin/uvicorn src.main:app --host 127.0.0.1 --port 5000 --reload",
    "check": "node --check frontend/js/app.js"
  }
}
